<h1> My Cocktails :)</h1>

<% @cocktails.each do |cocktail|%>
<!-- <ul>
<%= link_to cocktail.name,  cocktail_path(cocktail) %>
<%= link_to "Delete", cocktail_path(cocktail),
      method: :delete,
      data: { confirm: "Are you sure?" } %>
     
      <%= link_to "edit", edit_cocktail_path(cocktail)%>
</ul> -->
<table class="table">
  <thead>
    <tr>
      <th scope="col">#</th>
      <th scope="col">Name</th>
      <th scope="col">Delete</th>
      <th scope="col">Edit</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <th scope="row"></th>
      <td><%= link_to cocktail.name, cocktail_path(cocktail)%></td>
      <td><%= link_to "Delete", cocktail_path(cocktail),
      method: :delete,
      data: { confirm: "Are you sure?" } %></td>
      <td><%= link_to "edit", edit_cocktail_path(cocktail)%></td>
    </tr>
    
  </tbody>
</table>
<%end%>
<button type="button" class="btn btn-danger"><%= link_to "new",  new_cocktail_path%></button>


